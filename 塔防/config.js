const Config = {
    CtxScale: 0.5,
    TowerSize: 80,
    EnemySize: 40,
    MapSize: 100,
    BulletSize: 5
}
Config.TowerSelectX = (window.innerWidth / 2 - 250) / Config.CtxScale;
Config.TowerSelectY = (window.innerHeight - 50) / Config.CtxScale;
Config.InfoX = (window.innerWidth / 2 - 250) / Config.CtxScale;
Config.InfoY = 0;